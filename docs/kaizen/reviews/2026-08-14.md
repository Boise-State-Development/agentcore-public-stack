# Kaizen Review — Friday, August 14, 2026

> Prepared 11:05am MT. Review window: **July 3 – August 14** (42 days — six review cycles, not one).
> Source: research/2026-08-14.md + review-queue.md (**34 open** after hygiene; 46 before).
> Note: the queue-hygiene pass research/2026-08-14 asked for was pulled forward into the research PR (commit `a49d2656`, nine entries, at Phil's request) while this run was in progress. This pass resolved the three it left behind.
> **Scope narrowed 2026-08-14.** Mid-run, Phil narrowed the kaizen loop to **Strands Agents / AWS Bedrock / Bedrock AgentCore**, ranked on two questions: what custom code does an upstream release let us delete, and what can we now build on these three libraries that we couldn't before. Both skill files were updated and the proposals below were **re-ranked** under the new lens. Six previously-ranked items are routed under *Out of Scope*, not dropped.

## Week in Review

This is not a week — it is six. The last review ran **2026-07-03**; research ran on 07-10, 07-17 and 07-24, then skipped 07-31 and 08-07 and resumed today. In the gap the repo did not slow down: **43 PRs merged into `develop`, 151 non-merge commits, 7 releases (1.12.0 → 1.14.1), and zero CI failures in the last 12 days.** The single largest standing risk closed itself — `bedrock-agentcore` went 1.9.1 → **1.21.0** in #857, taking a bump that had been the queue's #1 item for seven consecutive weeks to **zero version lag**, with #482 and #571 both closed upstream.

The cost is visible in the queue. Four entries were still asserting *"we're on 1.9.1, exposed to #482 + #571 today"* — false since Aug 11 — and Phil cleared **nine** stale entries by hand this morning rather than let this ranking run consume them. Three items the 2026-07-03 review marked **Ship** (#405 docling, #399/#404 web-sources hardening, #480 Guardrails) are **all still open**, untouched, six weeks later. The loop caught real signal and then nothing consumed the decisions.

Externally the window was unusually coherent: **four independent vendors shipped four different answers to "the prompt cache is a design constraint"** — Anthropic's mid-conversation tool mutation that doesn't bust the prefix, Claude Code's fan-out staggering and cache-inheriting forks, MCP's server-advertised list TTLs, pydantic-ai's tools-hidden-until-revealed. We are ahead of all of them at *measuring* the problem (`cacheStatus`, fingerprint hashes, `partial_miss`, `wastedUsd`) and behind all of them at *escaping* it. Separately, MCP **2026-07-28 went final** and deleted the `initialize` handshake our MCP Apps host reads for the App-frame header — the window's one confirmed breaking change against shipped code.

## Friction — the week's signal

### Repeated patterns (≥2 occurrences)

- **Ship-recommended items don't ship** (3 occurrences, all from one review) — reviews/2026-07-03.md marked ten proposals; #6 (docling → close #405), #7 (web-sources helpers → close #399/#404) and #4 (Guardrails #480) were all **Ship**, and all three issues are still `OPEN` today. The two dependency bumps *did* land — six weeks late, and only as a side effect of #857's general upgrade sweep, not as a kaizen action.
  - *Hypothesis*: the review produces an agenda but there is no step that converts a ✅ into a tracked work item. Bumps eventually ship because they ride other work; issue-closing fixes have no such carrier.
  - *Candidate fix*: on a ✅, open the PR or a labelled issue **in the same session** as the decision. Anything not carried out of the review within a week goes back to the queue with an explicit revisit date rather than living in a merged doc.

- **Telemetry pointed at a source nothing writes to** (2 occurrences, different root causes) — PR #843 (Aug 5) fixed three Logs Insights widgets querying a phantom log group; #697's `cacheStatus` widget had **never** shown data and #838 copied the pattern. Then AWS flipped the Runtime unified-span destination default on **2026-07-20**, so any runtime recreated after that date writes spans to its own group.
  - *Hypothesis*: an empty query result reads as "no errors / no traffic" instead of "broken query". This is a silent-failure *class*, now recurred with a second, unrelated cause.
  - *Candidate fix*: every observability widget or Insights query asserts non-empty over a known-good window in CI, or carries a visible "0 results — verify source" affordance.

- **Derived execution contexts inherit permissions they were never granted** (4 occurrences, 3 ours + 1 upstream cluster) — #852 (`isPublic` was listing-only, never enforced), the RBAC half-write trap (`grantedSkills` alone grants nothing), the `@`-mention agent fork, and upstream Claude Code 2.1.222–2.1.228 fixing PreToolUse auto-allow hooks bypassing tool restrictions *in background agent tasks*.
  - *Hypothesis*: our access checks are correct at the primary turn and under-specified at every derived entry point.
  - *Candidate fix*: one targeted question, answered in code — does a **scheduled run** or an `@`-mentioned agent evaluate `grantedTools`/`grantedSkills` against the *invoking user's* role, or against whatever the parent turn resolved?

- **Users want an always-visible spend number** (2 occurrences, 3 days apart, independent) — [#860](https://github.com/Boise-State-Development/agentcore-public-stack/issues/860) "real-time usage counter against monthly allowance on-screen" and [#861](https://github.com/Boise-State-Development/agentcore-public-stack/issues/861) "automate end-user initiated quota increase requests", both 2026-08-12, both after 1.14.0 shipped the quota runway.
  - *Hypothesis*: the runway's rungs are **event-driven and dismissible**. We told users a wall exists without letting them see how close it is or do anything about it.
  - *Candidate fix*: an ambient indicator reading the same `totalCost` aggregate `quota_session_notice` already uses (no new aggregation), plus a request-increase action. Both pydantic-ai and Claude Code surface **the cap and its reset time** in-message — the piece the anchored-window spec still owes.

### One-offs worth watching

- **Revert `405d3526`** — the public Cognito client for CLI PKCE auth was removed from #848. A deliberate scope walk-back, not a failure; the Textual TUI client survived.
- **[#798](https://github.com/Boise-State-Development/agentcore-public-stack/issues/798) — two divergent `can_access_model` implementations (currently inert)** — filed Jul 30, still open. This is the exact class CLAUDE.md already names ("they previously diverged… so a model could be *listed* by the catalog and *denied* on use"). Inert today, but it re-arms the moment either branch changes.
- **[#797](https://github.com/Boise-State-Development/agentcore-public-stack/issues/797) — API keys survive account deprovisioning, no revocation path.** Open since Jul 30. Not surfaced by any research scan; found by internal audit only.
- **Uncommitted infra changes flagged by research this morning — now resolved.** The three loose IAM edits research warned were "one `git checkout` from being lost" landed in **#870** at 14:16 today. Working tree is clean apart from a scratch file. No action needed; noted so the risk isn't re-raised next week.

### Silence that matters

- **Zero comments on the last two kaizen PRs.** [#719](https://github.com/Boise-State-Development/agentcore-public-stack/pull/719) (research, Jul 24) and [#533](https://github.com/Boise-State-Development/agentcore-public-stack/pull/533) (review, Jul 3) both have **no comments and no reviews**. The skill's one-week POC feedback loop — Phil reviews Friday, POCs over the weekend, findings surface as PR comments the next Friday — produced **nothing this cycle**. Nine of the ten proposals below are therefore untested — the exception is Proposal #4, whose spike and live probe were run back in July and then never actioned. That is the single biggest quality gap in this document.
- **`.claude/skills/angualar-best-practices/` and `.claude/skills/frontend-design/`** — neither touched since **2026-04-27** (3.5 months). *Correction to research/2026-08-14.md*: it dated these to 2025-12-30 and 2026-01-18; `git log` puts the last commit touching both at 2026-04-27. Still stale, less dramatically so.
- **Nightly Build & Test: green Aug 3 → Aug 14, 12 for 12.** The dep-bump safety gate the queue has been worried about since June is live again, and it did its job — #857 shipped under it.
- **`duration_ms` tool-timing** — carried in the queue since 2026-05-15, surfaced in every review since, recommended **DROP** on 2026-07-03, and still sitting in `## Open`. Seventh cycle.

## Proposals — ranked

> **Re-ranked 2026-08-14 under the narrowed scope.** The kaizen loop is now scoped to **Strands Agents, AWS Bedrock, and Bedrock AgentCore**, ranked on two questions only: *what custom code does an upstream release let us delete?* and *what can we now build on these three libraries that we couldn't before?* Subtraction outranks unlock at equal effort. Six items from the first draft of this agenda were good work that answers neither question — they are routed under **Out of Scope**, not dropped. The change moved three long-buried queue items into the top five.

### 1. Adopt the Strands primitives already sitting in our 1.51 pin

- **Source**: research/2026-08-14.md ▸ Strands Agents (1.51 release contents) | direct observation
- **Scope**: Strands — **subtraction**, and the cheapest kind: no bump required
- **Surface area**: `agents/main_agent/` — `_build_filtered_tools` (our scoped-id `toolId::name` MCP filtering), the ContextOffloader spike's selection logic, per-tool hook sites
- **Change**: 1.51 — **the version we already run** — shipped selective offloading via a `should_offload` callback, MCP tool filtering with name prefixes, a snapshot session manager, `BeforeToolsEvent`/`AfterToolsEvent` batch hooks, and `estimateUtilization()` on the Model base class. Audit each against the custom code that does the same job today and delete what's now redundant. ⚠️ Diff the wheels — release notes are monorepo-wide and `CHANGELOG.md` is gone from `main`.
- **Subtracts**: yes, and it is the whole point — this is custom code we are maintaining for free from upstream. `_build_filtered_tools`' filtering and the offload-selection logic are the two named candidates.
- **Effort**: Low–Medium · **Impact**: Medium–High
- **POC findings**: not POCed.
- **Ship means**: one audit PR that names each 1.51 primitive, the code it duplicates, and either deletes ours or records why ours stays (a prompt-cache ordering constraint is a legitimate reason to keep custom filtering — but it should be *written down*, not assumed).
- **Decline means**: we keep hand-maintaining logic the library already ships, and the next `ContextOffloader` PR builds selection logic on top of a primitive that already exists.
- **Recommendation**: **Ship** — zero dependency risk, no bump, and it is the exact shape the narrowed loop exists to find. Under the old scope this sat below the cap.

### 2. Wire the Strands capabilities the 1.51 bump made available but never adopted

- **Source**: review-queue.md ▸ [2026-08-14] (split out of the resolved Strands bump entries)
- **Scope**: Strands — **subtraction**
- **Surface area**: `FilteredMCPClient` / Gateway targets (`continue_on_error`); `agents/main_agent/` hook registration (optional hook ordering, #2559)
- **Change**: `continue_on_error` so a flaky external/Gateway MCP server stops aborting the whole turn, replacing hand-rolled MCP-abort handling; deterministic hook ordering to sequence the OAuth-consent and tool-approval `BeforeToolCall` hooks through the tool-fold, replacing ad-hoc sequencing.
- **Subtracts**: yes — hand-rolled MCP-abort handling, and the sequencing the queued tool-approval work would otherwise have to invent.
- **Unlocks**: makes the tool-approval item tractable; the unattended Scheduled Runs lane stops dying on one bad external tool.
- **Effort**: Low–Medium · **Impact**: Medium
- **POC findings**: not POCed.
- **Ship means**: one PR. ⚠️ `decisions.md` [2026-05-18] bars a bare `context_manager="auto"` compaction swap — do not fold that in.
- **Decline means**: the bump's value stays half-collected; Scheduled Runs keep aborting unattended on a flaky Gateway target.
- **Recommendation**: **Ship** — the precondition has been met since #857 and the highest-blast-radius lane is the one it protects.

### 3. Probe Anthropic's mid-conversation tool-mutation beta on Bedrock

- **Source**: research/2026-08-14.md ▸ Top 5 #1 | review-queue.md (open since 2026-08-14)
- **Scope**: Bedrock — **probe**, with a conditionally enormous subtraction behind it
- **Surface area**: `agents/main_agent/core/model_config.py` (Bedrock request construction), `_build_filtered_tools`, the `toolConfigHash` fingerprint on `C#` rows, `GET /admin/costs/sessions/{id}/calls`
- **Change**: a **probe, not a build**. (a) Does Bedrock Converse expose the beta at all, or is it Claude-API-only? (b) If exposed, what is the header/parameter, and does `strands` pass it through? (c) On a real session, does adding a tool mid-conversation leave `cacheReadInputTokens` intact?
- **Subtracts**: conditionally, and enormously — the prompt-cache contract's most expensive clause ("any list that reaches `toolConfig` must be deterministically ordered at its source") narrows dramatically if mutation is cheap, and three queued items stop being blocked by the same wall.
- **Unlocks**: per-turn tool filtering — the real fix for MCP tool-list bloat; `@`-mention switches that don't structurally re-write a 30k–150k-token prefix.
- **Effort**: Low · **Impact**: High
- **POC findings**: not POCed.
- **Ship means**: run it against dev-ai on the existing experiment harness; record the answer in `docs/kaizen/scoping/` the way G1 and G3 were. **A negative result is a valid deliverable** — it closes an open assumption sitting under several queued items.
- **Decline means**: "never mutate `toolConfig`" stays an unexamined axiom.
- **Recommendation**: **Ship** — one day against the constraint that shapes this codebase, and squarely the "is it reachable on Bedrock?" question the narrowed scope explicitly keeps in.

### 4. Adopt the managed AgentCore Harness on the headless / scheduled lane

- **Source**: review-queue.md ▸ [2026-07-06] — **spike + live Q2 probe already complete**, `scoping/2026-07-06-managed-harness-spike-findings.md`
- **Scope**: AgentCore — **subtraction, the largest available**
- **Surface area**: `apis/shared/harness/run_agent_headless` (swap the Runtime `/invocations` target for `InvokeHarness`; swap the `sse.py` accumulator for a Converse-stream one → same `RunResult`) + infra (a managed-Harness resource with a 1:1 port of our existing `customJwtAuthorizer`). Interactive `inference-api` untouched.
- **Change**: move the proactive/scheduled lane onto managed Harness behind a flag. All three gating questions cleared for headless, and the Q2 probe ran live in dev-ai: `CreateHarness` accepts our exact authorizer, `customParameters` are honored verbatim, OAuth-inbound works, and a failed exchange surfaces legibly as a typed `runtimeClientError` → `paused_reauth`.
- **Subtracts**: potentially large — managed memory, **immutable versions + named endpoints (retires the ECR-tag / `update-function-code` fragility that bit the scheduled-runs deploy)**, automatic observability, `InvokeHarness` Step Functions composition, and execution limits, all as configuration rather than code.
- **Unlocks**: managed long-term memory and ops maturity for proactive agents without touching interactive chat; `export harness` keeps lock-in low.
- **Effort**: Medium · **Impact**: High
- **POC findings**: ✅ **the only POC-backed item on this board.** Spike complete, live probe run 2026-07-06 = **GO-with-boundary**. Boundary: the managed Gateway 3LO exchange fails with `ValidationException: You must provide a ResourceOauth2ReturnUrl` and does not source it from `defaultReturnUrl`, the callback header, or `UpdateWorkloadIdentity` — so keep `customParameters`-sensitive / 3LO connectors on our own `get_token_for_user` until AWS resolves the return-URL wiring.
- **Ship means**: adopt on the proactive lane behind a flag, with 3LO connectors excluded per the boundary. ⚠️ Managed memory is **on by default** — extra Memory cost per harness; price it against Proposal #5's numbers in the same pass.
- **Decline means**: log it — a completed spike with a live GO sitting unactioned for six weeks is a decision by default.
- **Recommendation**: **Ship** — highest-value item on the board by subtraction, and the only one where the investigation is already done. It was invisible under the old scope because it was competing with UI patterns.

### 5. Cut the AgentCore Memory long-term-strategy tier ($0.75 → $0.25)

- **Source**: research/2026-08-14.md ▸ Top 5 #3 | review-queue.md (open since 2026-08-14) | `project_cost_effectiveness_roadmap` (W5 is the named gap)
- **Scope**: AgentCore — **subtraction** (price, on records we already store)
- **Surface area**: `infrastructure/lib/constructs/agentcore/memory-construct.ts` — the `memoryStrategies` array (L79/L85/L91) and `eventExpiryDuration: 90` (L73); `apis/app_api/memory/routes.py`
- **Change**: we configure **all three built-in strategies** — `semanticMemoryStrategy`, `summaryMemoryStrategy`, `userPreferenceMemoryStrategy` — every one at the **$0.75/1,000 records/month** built-in tier versus **$0.25** for override/self-managed. Measure per-strategy record volume and read frequency, then decide per strategy whether 3× the rate buys anything.
- **Subtracts**: yes — a 3× per-record price cut on a line we pay the premium tier for **by default, with no deliberate decision behind it**.
- **Effort**: Medium · **Impact**: High
- **POC findings**: not POCed.
- **Ship means**: measure first — record and retrieval counts per strategy, priced against the $0.25 tier. Only then propose dropping or self-managing one.
- **Decline means**: runtime memory stays ~73% of the bill and W5 stays the named open gap.
- **Recommendation**: **Ship the measurement. Defer Runtime Instances four weeks (revisit 2026-09-11)** — Instances is the bigger prize on much weaker evidence (AWS's own GA announcement drew **1 point and 0 comments on HN**). Model it against real session-concurrency before it earns a slot.

### 6. Wire configurable Bedrock Guardrails — decide in-agent vs. gateway-level in one pass (#480)

- **Source**: review-queue.md (open since **2026-06-19**) | reviews/2026-07-03.md ▸ Proposal #4 (**Ship** — not actioned)
- **Scope**: Bedrock + AgentCore Policy — **capability unlock**
- **Surface area**: `BedrockModel` construction in inference-api + infrastructure (optional `CDK_GUARDRAIL_ID` / `CDK_GUARDRAIL_VERSION` threaded to runtime env; AgentCore Policy on the Gateway construct)
- **Change**: config-wire a capability **Strands already exposes** (`guardrail_id`/`version`/`stream_processing_mode`/`trace`), zero-cost when unset, mirroring the `CDK_ARTIFACTS_ENABLED` optional-feature pattern — and decide in the same pass whether one gateway-level policy is preferable to or complements the in-agent approach.
- **Subtracts**: partial — deciding both in one pass retires the separately-queued [2026-07-03] gateway-Guardrails entry rather than running two tracks.
- **Unlocks**: deployers attach content-safety filtering and staff alerting to all model invocations without touching inference-api source — the FERPA duty-of-care case. A gateway-level policy is model-independent and the agent cannot reason around it.
- **Effort**: Low–Medium · **Impact**: High
- **POC findings**: not POCed.
- **Ship means**: verify guardrail *resource* region availability and SSE streaming-mode compatibility, then wire the env vars. Note the standing caveat: **Mantle lacks Guardrails**, so this is a bedrock-runtime-path capability.
- **Decline means**: log it — an open issue with a library-native path, declined twice by inaction, deserves an explicit reason.
- **Recommendation**: **Ship or Decline — do not defer again.** Third review carrying it; the gap is a decision problem, not a priority problem.

### 7. Adopt Strands `Limits` on the unattended Scheduled Runs / headless lane

- **Source**: review-queue.md (open since 2026-07-17)
- **Scope**: Strands — **subtraction**
- **Surface area**: the headless/scheduled path (`apis/shared/harness/run_agent_headless`) + per-invocation agent-loop config
- **Change**: adopt the library-native per-invocation token/cost/turn cap on the lane where a runaway loop burns quota unattended, retiring any hand-rolled `max_turns` guard.
- **Subtracts**: yes — library-native replacement for a hand-rolled guard.
- **Unlocks**: a first-class per-invocation cost cap on the highest-blast-radius lane.
- **Effort**: Low–Medium · **Impact**: Medium
- **POC findings**: not POCed.
- **Ship means**: scope to the scheduled/headless lane only; sequence with Proposal #4, which touches the same file, and with the queued quota-cooldown work.
- **Decline means**: the unattended lane keeps relying on app-layer advisory quota alone.
- **Recommendation**: **Ship — bundle with #4.** Same lane, same file; shipping them separately pays the review cost twice.

### 8. Audit our Bedrock prompt-cache behaviour against measured upstream technique

- **Source**: research/2026-08-14.md ▸ Top 5 #4 | review-queue.md (open since 2026-08-14)
- **Scope**: Bedrock — **subtraction** (find waste and delete it). In scope because every finding is verifiable against our own Converse traffic.
- **Surface area**: `core/model_config.py` (cache-point placement, `CacheConfig(strategy="auto")` at L389), `session/turn_based_session_manager.py` (truncation anchor, repeated-compaction path), `session/compaction_models.py`, system-prompt assembly
- **Change**: four checks, fix what they find, add nothing. (1) **byte-stable prefix** — grep system-prompt assembly for anything time-, random-, or environment-derived; upstream measured a **44% swing from a single `datetime.now()`**, and this doubles as a test of whether we actually read `systemPromptHash`. (2) **layered mixed-TTL breakpoints** — 54% cheaper upstream but **blocked by Strands #3758**, which makes per-section TTLs a hard `ValidationException`; evaluate and document, do not adopt. (3) **repeated-compaction audit** — does pass 2 preserve tool pairing without re-writing the prefix? (4) **retry cap** — an uncapped retry on a 424 or throttle re-writes the whole prefix per attempt.
- **Subtracts**: yes — every item deletes waste. No new abstraction, no new dependency.
- **Effort**: Low · **Impact**: Medium–High
- **POC findings**: not POCed.
- **Ship means**: one PR fixing items 1, 3 and 4, plus a note in the cost-effectiveness roadmap recording item 2's blocked status and the #3758 landmine.
- **Decline means**: we keep the instrumentation and keep not reading it — the pattern PR #843 already caught once.
- **Recommendation**: **Ship** — highest confidence-per-hour item, and the measurement apparatus already exists.

### 9. Migrate the MCP Apps host off `initialize`/`serverInfo` to `server/discover`

- **Source**: research/2026-08-14.md ▸ Top 5 #2 | review-queue.md (open since 2026-08-14)
- **Scope**: MCP through the AgentCore-Gateway / MCP-host aperture — **subtraction**. In scope precisely because it touches our host and Gateway-fronted servers; general MCP-ecosystem news is not.
- **Surface area**: `agents/main_agent/integrations/mcp_apps.py` (the `serverInfo` capture and its consumers), `streaming/stream_coordinator.py` (`ui_resource` header emission), the `ClientCapabilities(experimental=...)` subclassing
- **Change**: resolve `serverName`/`icon` from `server/discover`, keeping the `manifest.json` fallback as tier two and the title-cased `ui://` authority as tier three. Keep the handshake as a compatibility branch — `server/discover` is **mandatory for servers, optional for clients**.
- **Subtracts**: yes — retires the `initialize`-response dependency, and collapses the "fresh MCP session per call" concern behind the proxy-call 504 work, because **there is no protocol-level session left to preserve**.
- **Unlocks**: readiness for **MRTR (SEP-2322)** — the sanctioned interrupt/resume shape, which would let OAuth consent and tool approvals resume *without holding an SSE stream open* against the 600s timeout; readiness for SEP-2243 header-based Gateway routing.
- **Effort**: Medium · **Impact**: Medium–High
- **POC findings**: not POCed.
- **Ship means**: **two verifications before any code** — confirm the UI capability identifier against the apps **spec source** (this scan could not confirm `io.modelcontextprotocol/ui` vs `experimental.ui`), and confirm `ui/notifications/tool-input-partial` still exists, since our relay depends on it. Then the migration.
- **Decline means**: App frames silently lose `serverName` and `icon` as servers upgrade — **degrades quietly, never errors**.
- **Recommendation**: **Ship the two verifications this week; defer the migration until they land.** Building against an unconfirmed identifier is the expensive mistake here.

### 10. Local guard for `bedrock-agentcore` #564

- **Source**: review-queue.md ▸ [2026-08-14] (residue of the resolved bump entries)
- **Scope**: AgentCore — **neither question, honestly**: this is covering a library *gap*, the inverse of subtraction. In scope because it is AgentCore-library work on a live cost bug, and it is explicitly deleted the day upstream fixes it.
- **Surface area**: `TurnBasedSessionManager` restore path — the metadata-filtered `ListEvents` read and the "is this a new session?" decision it drives
- **Change**: guard against a transient `ListEvents` inconsistency making the manager miss marker events and treat the turn as a **new session** — re-read unfiltered, or refuse to conclude "new session" when a session row exists.
- **Subtracts**: no — a temporary guard, deleted when [#564](https://github.com/aws/bedrock-agentcore-sdk-python/issues/564) closes. Flagged rather than dressed up.
- **Effort**: Low–Medium · **Impact**: Medium–High
- **POC findings**: not POCed.
- **Ship means**: the guard plus a code comment naming #564 so the deletion trigger is discoverable. Check #621 (`filter_restored_tool_context` vs extended thinking) on the same path in the same pass.
- **Decline means**: accept silent context loss **and** a full cacheable-prefix re-write on an unknown fraction of turns.
- **Recommendation**: **Ship** — this is a prompt-cache cost bug before it is a correctness one, and it is the one hazard the 1.21.0 bump did not close.

## Out of Scope — routed elsewhere

The narrowing means good work now leaves the queue through a named door instead of being re-ranked forever. These six were ranked proposals in this agenda's first draft; all are real, none are kaizen's.

- **Composer draft recovery on failed send** (L × M, frontend) — the missing half of 1.14.1: the single-flight guard correctly rejects a duplicate turn and still eats the user's typed text. Not a library capability. → *routed*: **open an issue and ship it anyway** — it's an afternoon and the highest user-visible value of anything reviewed today. It just isn't kaizen's to track.
- **Ambient quota indicator + self-service increase** — [#860](https://github.com/Boise-State-Development/agentcore-public-stack/issues/860) and [#861](https://github.com/Boise-State-Development/agentcore-public-stack/issues/861), two independent users in three days. Product work on our own quota surface. → *routed*: **already filed**; fold into the drafted cooldown-windows spec so the indicator and the anchored windows agree on one model of "how much is left and when does it reset".
- **Derived-context RBAC audit** — do scheduled runs and `@`-mentioned agents evaluate `grantedTools`/`grantedSkills` against the *invoking user's* role, or the parent turn's? Four prior occurrences (#852, the RBAC half-write trap, the `@`-mention fork, and [#798](https://github.com/Boise-State-Development/agentcore-public-stack/issues/798)). Internal correctness, not library work. → *routed*: **open an issue**, and close or re-scope #798 in the same pass.
- **`docling` bump → close [#405](https://github.com/Boise-State-Development/agentcore-public-stack/issues/405)** — `.txt` uploads have been broken **ten weeks** across three reviews. `docling` is not one of the three libraries. → *routed*: **just fix it** — it is a one-sitting bug, and carrying it a fourth time through a governance forum is the actual waste.
- **SPA SSE parser hardening** ([2026-07-17], from assistant-stream) and the **interactive context-breakdown badge** ([2026-06-19], from Cursor/LibreChat) — both sourced from now-out-of-scope frontend references. → *routed*: **open issues**; both are defensible frontend work on surfaces we've shipped.
- **Tool-approval SSE/part state + signed approvals** ([2026-07-03] / [2026-07-10], from Vercel AI SDK) — **split**: the `BeforeToolCall` hook-ordering half is in scope and is inside Proposal #2; the SSE-contract and UI-state half is not. → *routed*: **open an issue for the UI half**. The arg-binding HMAC idea (bind the approval signature to tool name, call ID *and* input args, so an approved call can't be replayed mutated) is worth keeping in that issue.
- **The curl version pin**, **Angular 21→22 / TypeScript 5.9→7 lag**, and **named A2A participants in the chat UI** — Docker hygiene, supporting-tier dependency lag, and frontend attribution respectively. → *routed*: **issues**. The TypeScript gap (two majors, ~10.5 months) is the largest in the pin table and is downstream of the Angular decision — it wants its own scoping pass, not a kaizen slot.
- **Restore the weekly kaizen cadence** — two research Fridays and six reviews were missed, which is how three Ship-recommended items rotted and four queue entries went factually false. → *routed*: **partly addressed by this narrowing** (fewer sources, shorter runs, sharper output). The trigger question — why 07-31 and 08-07 produced nothing — still needs a look, but it is process, not an agenda item.

## Carried Over From Prior Reviews

- **AgentCore Runtime BYO filesystem (S3 Files / EFS)** (deferred 2026-05-15 → 2026-06-12 → 2026-08-01) — **now due, and now clearly in scope** (AgentCore Runtime capability unlock). *New context*: Runtime **Instances** GA brings 14-day persistent sessions; the reference repo shipped a workspace / Code-Interpreter cluster including a **storage-neutral session file API**; our own session workspace tools PR-1 is built. *Recommendation*: **fold into a single agent-workspace / code-exec ADR** together with the workspace-tools PR-2 scope and Proposal #5's Instances track. Revisit 2026-09-11. ⚠️ Three of the reference repo's six workspace commits are **security fixes** (path normalization without regex backtracking, hashed storage keys, hardened file access) — check those before PR-2 if our workspace tools expose user-supplied paths.
- **`oauth_required` SSE flow audit** (deferred 2026-05-10 until 2026-05-24) — **~12 weeks overdue, fourth surfacing.** *New context*: SEP-2322 shipped final as **MRTR** — the sanctioned interrupt/resume shape, no held-open stream. *Recommendation*: **fold into Proposal #9** as its MRTR-readiness half and delete the standalone entry. It must not carry a fifth review.
- **`duration_ms` tool-timing into `tool_result` SSE** (carried since 2026-05-15; recommended **DROP** on 2026-07-03, never actioned) — **seventh cycle**, and now out of scope besides. *Recommendation*: **Decline** to `decisions.md` — "repeatedly out-prioritized; context attribution shipped without it; out of scope after the 2026-08-14 narrowing."
- **Named A2A agent participants in the chat UI** — precondition (an A2A *server* construct) still unmet; the UI half is now out of scope and routed above. *Keep in scope*: when a server construct lands it must advertise `capabilities` with `streaming=True` (already in CLAUDE.md) **and** account for `bedrock-agentcore` [#583](https://github.com/aws/bedrock-agentcore-sdk-python/issues/583) — `serve_a2a` never hits the AgentCore idle session timeout, reintroducing the runaway-microVM class the idle reaper (#827) just fixed. *Recommendation*: **record #583 in CLAUDE.md beside the streaming guard** so both traps are caught in one read.

## Retirement Candidates

- **Twelve superseded queue entries — retired today, in two passes.** Commit `a49d2656` on the research PR cleared **nine** (four `bedrock-agentcore` bumps, two Strands bumps, two nightly-CI entries, two MCP Apps spec-prep entries), carrying the real residue forward. This pass cleared the **three** it left — the hygiene entry itself (status said ✅ DONE while sitting in `## Open`) and the two duplicate caching-audit entries. **Queue: 46 → 34 open, with no false premises.**
- **Roughly a third of the remaining queue is now out of scope.** The routing list above is the start; the next review should finish the pass so the queue only holds Strands/AgentCore/Bedrock work. That is a larger subtraction than anything in the Proposals list.
- **The two research-source categories the narrowing deleted** — agentic UI/UX pattern sources (Vercel AI SDK, assistant-ui, NN/g, Linear/Cursor) and competing-harness sources (Claude Code, opencode, LangChain, pydantic-ai, LibreChat), plus HN/Reddit community signal. Historically they produced the most words per actionable item; the 2026-08-14 scan spent roughly half its 71-request budget on them and produced two proposals, both now routed out.
- **`.claude/skills/angualar-best-practices/`** — last touched 2026-04-27; the directory name is **misspelled**, so any reference to `angular-best-practices` silently misses it, while Angular sits a full major behind.
- **`.claude/skills/frontend-design/`** — last touched 2026-04-27; duplicated by the `anthropic-skills:frontend-design` plugin skill available in-session.

## Risks Acknowledged But Not Acted On

- **MCP 2026-07-28 deleted the `initialize` handshake our MCP Apps host reads** — https://modelcontextprotocol.io/specification/versioning — *what breaks*: a server that upgrades stops returning `serverInfo`, so App frames silently fall back to a title-cased `ui://` authority and a generic glyph. **Degrades quietly, never errors.** Mitigating: `server/discover` is optional for clients, so servers must keep serving handshake-era clients through the transition. — recommendation: **Address now** via Proposal #7's two verifications.
- **`bedrock-agentcore` #629 — end-of-invocation spans dropped when the microVM freezes** — https://github.com/aws/bedrock-agentcore-sdk-python/issues/629 — *what breaks*: cost and cache telemetry **under-reports the final turn of every session**, which is exactly where compaction and cache-write spikes land. Any EMF/trace-derived conclusion about end-of-session cost may be systematically low. — recommendation: **Watch until 2026-09-11**; if unfixed, prefer the DynamoDB `C#` rows over trace data for cost conclusions and say so in the roadmap.
- **AWS flipped the Runtime span destination default on 2026-07-20** — https://docs.aws.amazon.com/bedrock-agentcore/latest/devguide/observability-configure.html — *what breaks*: a runtime recreated after that date writes spans to its own log group, and a query pointed at `aws/spans` returns **empty rather than erroring** — the same silent-failure class PR #843 just fixed for a different cause. — recommendation: **Address now** — a 20-minute check of which group our runtimes actually write to, folded into whoever touches observability next.
- **`bedrock-agentcore` #564 remains open** — https://github.com/aws/bedrock-agentcore-sdk-python/issues/564 — *what breaks*: a transient `ListEvents` inconsistency makes the manager treat a turn as a new session, silently losing context **and** re-writing the entire cacheable prefix. This is the one of our three tracked issues the 1.21.0 bump did **not** close. — recommendation: **Address now** — carried forward as its own queue entry; a local guard is cheaper than waiting on upstream.
- **Strands #3758 — per-section cache TTLs emit a checkpoint order Bedrock rejects on every request** — https://github.com/strands-agents/sdk-python/issues/3758 — *what breaks*: nothing today (we set no per-section TTL), but it is a hard `ValidationException` landmine directly under the cookbook's 54%-cheaper layered-TTL technique. — recommendation: **Accept + document** inside Proposal #2.
- **A floating model alias would silently re-point production traffic** — OpenAI reaffirmed that `chat-latest` now tracks the newest ChatGPT model while **GPT-5.6 Sol remains the production API recommendation**. *What breaks*: a non-pinned identifier in the Mantle registry re-writes the cacheable prefix on whatever day OpenAI moves it — the exact invisible regression `systemPromptHash`/`toolConfigHash` exist to catch. — recommendation: **Address now** — a one-grep audit of the Mantle model registry for any unpinned identifier.

## What Shipped This Week

*(42-day window — 43 PRs merged, 7 releases 1.12.0 → 1.14.1. Highlights only.)*

- **#857 — `strands-agents` 1.47 → 1.51.0 and `bedrock-agentcore` 1.9.1 → 1.21.0** — *the seven-week queue-topping item; takes us to zero version lag and closes #482 and #571 upstream.*
- **#863 / #864 — dropped SSE streams no longer brick the next message; mid-stream disconnects are attributable** — *closed the lease-leak class and made the next one diagnosable.*
- **#858 / #859 — cancellation reaches in-flight MCP calls and doesn't outlive the turn; Memory writes offloaded off the asyncio event loop** — *fixed the sticky-cancel bug that bricked a whole session.*
- **#827 — armed AgentCore's idle reaper in `/ping`** — *microVM lifetimes fell from 480–520 min to 18–50; the largest single cost lever shipped this window.*
- **#845 — quota runway: earlier warning rungs plus a per-session cost notice** — *and the direct cause of #860/#861 asking for more.*
- **#838 / #843 — `partial_miss` prefix classification, and three Logs Insights widgets repointed at the real log group** — *the instrument and the discovery that we weren't reading it.*
- **#852 / #808 / #807 / #796 — RBAC: `isPublic` enforced, durable role-mutation audit trail, delegated admin visibility, API-key owner role resolution.**
- **#841 / #839 — conversations pinned to a microVM via runtime session id; agents carrying artifact tools cached** — *the G1 experiment arms; the cost thesis was disproven and a latency win found instead.*
- **#823 — pre-merge guard for DynamoDB's one-GSI-per-`UpdateTable` limit** — *turned a repeated deploy trap into a CI gate.*
- **#828 / #824 / #800 / #799 — marketplace: live-listing updates, `taken_down → private`, version rollback, four E2E snapshot gaps.**

## Take

The system is trending **toward trust** and the *loop* was trending toward friction — an unusual split, and the narrowing is the right response to it. The code got materially better in six weeks: zero dependency lag, twelve straight green nights, the idle-reaper cost fix, three stream-reliability classes closed. But the kaizen loop stopped consuming its own output, and part of why is now visible: the old scope spread ~71 web requests across fifteen source categories, and the two proposals that came out of the broadest half of them are the two routed out of this agenda entirely.

Re-ranking under the two questions did something the old lens could not — it surfaced **Proposal #4, the managed AgentCore Harness**, from the queue where it had been sitting since July 6 with a completed spike and a live GO-with-boundary probe. That is the largest available subtraction in the repo (managed memory, immutable versions and named endpoints retiring the ECR-tag fragility, auto observability, execution limits — all as config), it is the only POC-backed item on the board, and under the old scope it was competing for attention with UX pattern references. That is the clearest evidence the narrowing is correct.

The one change that matters most this week is **Proposal #1** — adopting the Strands primitives already in the 1.51 pin. No bump, no dependency risk, and it deletes code we are currently maintaining for free from upstream. If bandwidth allows exactly two, pair it with **#3** (the Bedrock tool-mutation probe), which costs a day and either unblocks three queued items or closes the question for a quarter.

One caveat carried forward: the narrowed scope will produce **shorter, drier research docs**, and some weeks will genuinely have nothing. That is the intended trade. The failure mode to watch for is a scan that pads to fill the Top 5 rather than reporting a quiet week — the skill now says so explicitly, and it is worth holding it to that.

---

## Review Protocol (for Phil)

1. Read Friction (2 min). The headline is *Ship-recommended items don't ship*: three items this forum marked ✅ on 2026-07-03 are still open today.
2. **Sanity-check the narrowing first** (1 min) — read the *Out of Scope* list. If anything there should have stayed a kaizen proposal, say so now; that judgment shapes every future run, not just this one.
3. Scan Proposals — mark ✅ Ship / ❌ Decline / ⏸ Defer on each (4–6 min). **10 proposals**, seven of them subtractions; recommendation: **ship #1 (Strands primitives already in our pin) + #3 (Bedrock tool-mutation probe)**, and make a real call on **#4 (managed Harness)** — its spike has been done since July. Bundle **#7** with **#4** (same lane, same file), force a decision on **#6 (Guardrails)** either way, and sequence **#9** behind its two verifications.
4. Scan Retirement Candidates — same marks (1–2 min). Two dead skills; the twelve queue retirements are already executed (nine by you this morning, three here).
5. Resolve Carried Over items (2 min). **Four**, three of them multiple cycles overdue — `oauth_required` and `duration_ms` both need a terminal decision, not a fifth deferral.
6. Resolve the Risks block — two are marked **Address now** and cost under 30 minutes each.
7. Pick 1–3 to ship this week. Decline or defer the rest with a reason **and open the PR or labelled issue in the same session** — that's the fix for this week's headline friction.

Target: **12–15 minutes** (longer than usual — this covers six cycles, not one).

## Post-review (for Phil — separate PRs)

- ✅ Ship items → individual feature PRs over the week. The decision is logged here; the implementation lives elsewhere.
- ❌ Decline items → appended to `docs/kaizen/decisions.md` with the reason so future research doesn't re-propose.
- ⏸ Defer items → kept open in `review-queue.md` with a "revisit by [date]"; surfaced again in the next review when due.
